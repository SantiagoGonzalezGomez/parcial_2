name: parcial_flutter
description: Aplicación universitaria - Parcial Flutter

publish_to: 'none'
version: 1.0.0+1

environment:
   sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  dio: ^5.3.2
  go_router: ^12.1.1
  fl_chart: ^0.65.0
  skeletonizer: ^0.7.2
  image_picker: ^1.0.4
  flutter_dotenv: ^5.1.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env